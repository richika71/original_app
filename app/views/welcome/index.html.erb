<div class="text-center">
    <% if user_signed_in? %>
        <h1><%= t('hello') %> <strong><%= current_user.name %></strong>!</h1>
    <% else %>
        <h1><%= t('hello') %></h1>
    <% end %>
    <p><%= Date.today %></p>
    <%= link_to 'Look Posts', articles_path %>
</div>
